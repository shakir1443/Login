# Login
A php MySQL login page

## Database 
In phpmyadmin, Create a databse with the name "Login". After that create a table with the name "admin" with three fields.
Those are :
1. id(Auto_increment, Primary, int)
2. username(varchar)
3. passcode(varchar)
